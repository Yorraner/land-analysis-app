# 🏗️ 土地整治智能文档分析系统

这是一个基于 Streamlit 的智能文档处理平台，集成了 PDF 自动裁剪、Coze API 数据提取、结构化解析以及多源数据融合功能。

## ✨ 功能特性

文档处理：自动识别 PDF 章节（如“存在问题”、“整治潜力”）并裁剪为独立文件；支持手动指定页码修复。

数据获取：批量调用 AI (Coze) 接口提取非结构化数据。

数据解析：内置多种解析器（正则提取、Markdown 表格解析），将 AI 返回的脏数据清洗为结构化表格。

数据融合：自动对齐不同来源的地区数据，生成 $N \times d$ 特征矩阵。

## 🚀 快速开始

### 本地运行

安装依赖：

>pip install -r requirements.txt


### 启动应用：

>streamlit run app.py


部署到 Streamlit Cloud

Fork 或 Clone 本仓库到您的 GitHub。

访问 Streamlit Cloud 并连接您的 GitHub 账号。

选择本仓库，主文件路径填写 app.py。

点击 Deploy 即可上线。

📁 目录结构

app.py: 应用主入口

utils_pdf.py: PDF 处理核心工具 (PyMuPDF)

utils_parsers.py: 数据清洗与正则解析工具

utils_fusion.py: 数据融合与矩阵构建工具

api_client.py: API 调用客户端 (目前为模拟模式)